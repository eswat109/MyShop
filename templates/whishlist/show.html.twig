{% extends 'base.html.twig' %}

{% block title %}Whishlist{% endblock %}

{% block body %}
    <h1>Whishlist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whishlist.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('whishlist_index') }}">back to list</a>

    <a href="{{ path('whishlist_edit', {'id': whishlist.id}) }}">edit</a>

    {{ include('whishlist/_delete_form.html.twig') }}
{% endblock %}
